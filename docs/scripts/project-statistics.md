# Statistik-Skriptregister / Statistics script registry

Das Repository besitzt keinen zentralen JSON-Skriptkatalog. Dieses lokale
Register ordnet die neue Automation der Kategorie Statistik zu.

This repository has no central JSON script catalog. This local register assigns
the new automation to the statistics category.

| Skript / Script | Zweck / Purpose | Hilfe / Help |
| --- | --- | --- |
| `scripts/prove-project-statistics-context.ps1` | Read-only Quellenpruefung und isolierte Tests / Read-only source proof and isolated tests | [Manpage](../man/prove-project-statistics-context.1.md) |

Die wiederverwendete PowerShell-7-Automation wird auf allen Plattformen direkt
ausgefuehrt; kein neuer Shell-Wrapper. Kein Commit, Push, Merge oder Freigabe.

Run the reused PowerShell 7 automation directly on all platforms; no new shell
wrapper, automatic Git delivery or human acceptance.
